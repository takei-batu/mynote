name: Simple Workflow
on:
  # push:
  #   branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run a script
        run: echo "succeeded push !"